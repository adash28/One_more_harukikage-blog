# 再弹一曲春日影个人网站 - 使用和维护指南

## 📋 目录

1. [功能概览](#功能概览)
2. [改动说明](#改动说明)
3. [配置指南](#配置指南)
4. [日常维护](#日常维护)
5. [常见问题](#常见问题)
6. [技术栈](#技术栈)

---

## 🎯 功能概览

本次更新为您的个人网站添加了以下功能：

### 1. ✨ 全宽顶部导航栏
- **效果**：导航栏现在覆盖整个页面宽度，更加舒展美观
- **特点**：响应式设计，在不同屏幕尺寸下自动适配

### 2. 🌟 全屏欢迎页面
- **效果**：访客首次进入网站时，会看到全屏欢迎界面
- **内容**：包含欢迎Logo、欢迎标语、副标题和动态粒子特效
- **交互**：点击任意处进入主网站，仅在每个会话显示一次

### 3. 💬 评论系统（Giscus）
- **功能**：基于 GitHub Discussions 的评论系统
- **权限**：作为仓库所有者，您拥有完全的管理权限（删除、编辑评论）
- **特点**：支持主题自动切换、Markdown 格式、表情反应

---

## 📝 改动说明

### 新增文件

```
webapp/
├── layouts/
│   ├── _default/
│   │   ├── baseof.html          # 自定义基础模板（添加欢迎页面）
│   │   └── single.html          # 自定义文章模板（集成评论）
│   └── partials/
│       └── comments.html        # 评论系统组件
├── assets/
│   └── css/
│       └── extended/
│           └── custom.css       # 自定义样式（导航栏、欢迎页面、评论区）
└── static/
    └── images/
        └── welcome-logo.svg     # 默认欢迎Logo
```

### 修改文件

- **hugo.toml**：添加了欢迎页面和评论系统配置

### 设计原则

✅ **保留原有功能**：所有改动都是扩展性的，不影响原有内容和布局  
✅ **主题兼容性**：通过 layouts 目录覆盖，不修改主题源文件  
✅ **响应式设计**：所有新功能在移动端和桌面端都有良好表现  
✅ **可配置性**：通过 hugo.toml 轻松控制各项功能

---

## ⚙️ 配置指南

### 1. 欢迎页面配置

在 `hugo.toml` 中修改欢迎文字：

```toml
[params]
    welcomeTitle = "您的欢迎标题"
    welcomeSubtitle = "您的副标题"
```

**更换欢迎Logo**：
- 将您的图片放在 `static/images/` 目录
- 重命名为 `welcome-logo.svg`、`welcome-logo.jpg` 或 `welcome-logo.png`
- 推荐尺寸：150x150 像素，圆形或方形

**禁用欢迎页面**：
如果您不想显示欢迎页面，可以：
1. 删除 `layouts/_default/baseof.html` 文件
2. 或者修改该文件，删除 `{{- if .IsHome }}` 到 `{{- end }}` 之间的欢迎屏幕代码

### 2. 评论系统配置

#### 步骤 1: 启用 GitHub Discussions

1. 访问您的仓库：https://github.com/adash28/One_more_harukikage-blog
2. 点击 "Settings" → "General"
3. 在 "Features" 部分，勾选 "Discussions"

#### 步骤 2: 获取 Giscus 配置

1. 访问 https://giscus.app/zh-CN
2. 在 "仓库" 输入框填写：`adash28/One_more_harukikage-blog`
3. 选择 "页面 ↔️ discussion 映射关系"：推荐 `pathname`
4. 选择 "Discussion 分类"：推荐 `Announcements`
5. 向下滚动，复制生成的配置参数

#### 步骤 3: 更新 hugo.toml

将获取的参数填入 `hugo.toml`：

```toml
[params.giscus]
    repo = "adash28/One_more_harukikage-blog"
    repoID = "您的仓库ID"              # 从 giscus 网站获取
    category = "Announcements"
    categoryID = "您的分类ID"          # 从 giscus 网站获取
    mapping = "pathname"
    strict = "0"
    reactionsEnabled = "1"
    emitMetadata = "0"
    inputPosition = "bottom"
    theme = "preferred_color_scheme"
    lang = "zh-CN"
    loading = "lazy"
```

#### 管理评论权限

作为仓库所有者，您可以：
- ✅ 删除任何评论
- ✅ 编辑或锁定 Discussion
- ✅ 标记垃圾评论
- ✅ 设置审核规则

访问仓库的 Discussions 页面进行管理：
https://github.com/adash28/One_more_harukikage-blog/discussions

### 3. 导航栏配置

导航栏现在自动全宽显示。如需调整最大内容宽度：

编辑 `assets/css/extended/custom.css`：

```css
.nav {
    max-width: 100% !important;
    padding: 0 max(calc((100vw - 1200px) / 2), 20px);  /* 1200px 是内容最大宽度 */
}
```

---

## 🔧 日常维护

### 添加新文章

```bash
# 进入项目目录
cd /home/user/webapp

# 创建新文章（评论功能默认启用）
hugo new content/your-category/your-post.md

# 如果不想在某篇文章显示评论，在 front matter 添加：
# hideComments: true
```

### 本地预览

```bash
cd /home/user/webapp

# 启动开发服务器
hugo server -D

# 访问 http://localhost:1313
```

### 构建和部署

```bash
# 构建静态文件
cd /home/user/webapp && hugo

# 提交到 GitHub
cd /home/user/webapp && git add .
cd /home/user/webapp && git commit -m "Update content"
cd /home/user/webapp && git push origin main
```

### 更新主题

```bash
cd /home/user/webapp

# 如果主题是子模块
git submodule update --remote themes/PaperMod

# 提交更新
git add themes/PaperMod
git commit -m "Update PaperMod theme"
git push
```

---

## ❓ 常见问题

### Q1: 欢迎页面每次都显示怎么办？

**A**: 欢迎页面使用 `sessionStorage` 记录，每个浏览器会话只显示一次。如果您想让它只显示一次（即使关闭浏览器），可以修改 `layouts/_default/baseof.html`，将 `sessionStorage` 改为 `localStorage`。

### Q2: 评论不显示怎么办？

**A**: 检查以下几点：
1. 确保仓库启用了 Discussions 功能
2. 确认 `hugo.toml` 中的 `repoID` 和 `categoryID` 正确
3. 检查文章的 front matter 中是否设置了 `hideComments: true`
4. 查看浏览器控制台是否有错误信息

### Q3: 导航栏在移动端显示异常？

**A**: 清除浏览器缓存，或强制刷新（Ctrl+Shift+R / Cmd+Shift+R）。

### Q4: 如何修改欢迎页面的颜色？

**A**: 编辑 `assets/css/extended/custom.css` 中的以下部分：

```css
#welcome-screen {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    /* 修改为您喜欢的渐变色 */
}
```

### Q5: 如何禁用某个页面的评论？

**A**: 在该页面的 front matter 中添加：

```yaml
---
title: "文章标题"
hideComments: true
---
```

### Q6: 网站出现 502 错误怎么办？

**A**: 
1. 检查 `hugo.toml` 语法是否正确（特别是引号和缩进）
2. 运行 `hugo` 命令检查是否有构建错误
3. 确保所有自定义模板文件语法正确
4. 查看 GitHub Actions 构建日志

---

## 🛠️ 技术栈

- **静态站点生成器**: Hugo v0.146.0+
- **主题**: PaperMod
- **评论系统**: Giscus (基于 GitHub Discussions)
- **部署平台**: GitHub Pages
- **版本控制**: Git

---

## 📞 获取帮助

如果遇到问题：

1. **查看 Hugo 文档**: https://gohugo.io/documentation/
2. **PaperMod 主题文档**: https://github.com/adityatelange/hugo-PaperMod/wiki
3. **Giscus 文档**: https://giscus.app/zh-CN
4. **GitHub Discussions**: 在您的仓库创建讨论帖

---

## 📅 版本历史

### v1.1.0 (2026-02-04)

**新功能**：
- ✅ 全宽顶部导航栏
- ✅ 全屏欢迎页面（带粒子特效）
- ✅ Giscus 评论系统集成

**改进**：
- ✅ 响应式设计优化
- ✅ 主题自动切换支持
- ✅ 移动端体验提升

---

## 🎨 自定义建议

### 进阶自定义

如果您想进一步定制网站：

1. **修改配色方案**：编辑 `assets/css/extended/custom.css`
2. **添加更多特效**：在 `layouts/_default/baseof.html` 中添加 JavaScript
3. **自定义评论样式**：在 `layouts/partials/comments.html` 中调整
4. **添加页脚信息**：创建 `layouts/partials/footer.html`

### 备份建议

定期备份以下重要文件：
- `hugo.toml`（配置文件）
- `content/`（所有文章内容）
- `layouts/`（自定义模板）
- `assets/css/extended/`（自定义样式）
- `static/images/`（图片资源）

---

**祝您使用愉快！** 🎉

如有任何问题，欢迎在仓库的 Discussions 或 Issues 中提出。
